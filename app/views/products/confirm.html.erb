<h1>Confirm Purchase</h1>
<br>
<table class="table table-hover" style="width:60%">
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Quantity</th>
  </tr>
  <tr>
    <td><%= @product.name.capitalize %></td>
    <td><%= @product.price %></td>
    <td><%= 1 %></td>
  </tr>
</table>
<%= button_to "Purchase", @product, method: :post %>
<br>
<%= link_to('Back', :action=>'purchase')%>
